import { createTheme } from '@mui/material'

export const dashboardTheme = createTheme({
    palette: {
        background: {
            default: '#f5f7fb',
        },
    },
    shape: {
        borderRadius: 20,
    },
})

